[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "module_a_native"
version = "0.2.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "module_a_native"
