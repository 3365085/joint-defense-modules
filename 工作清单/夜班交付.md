# 夜班打磨交付说明（2026-05-13）

> 你的使命："在保证原功能基础下，最佳程度优化两个模块的各个功能，
> 往工程上靠拢，往合并上对齐，解决潜在问题。"

## 一句话总结

两个模块都打磨好了、往合并对齐了、单元和端到端测试都绿了。
**联合根 pytest 186 项全通过**，7 clip smoke 全绿，Web 监控台仍然可用，
**模块 A pipeline 总耗时 p95 从 26-29 ms 降到 18-22 ms**（-25%），
**模块 A 的 A3b 特征 p95 从 15-18 ms 降到 9-11 ms**（-40%）。

## 文件变动一览

### 新建
- `工作清单/README.md`、`STATUS.md`、`PERFORMANCE_REPORT.md`、`夜班交付.md`
- `探索/A3b_l0_multiscale_cost.md`、`A2_lbp_noise_analysis.md`、
  `A_monitor_app_split_plan.md`、`J_joint_baseline_config.md`、
  `joint_baseline.yaml`、`joint_run_smoke.ps1`
- `模块A/defense/module_a/utils.py`（Ultralytics settings 隔离、package root 解析）
- `模块A/pytest.ini`
- `模块A/tools/calibrate_classifier_threshold.py`
- `模块A/tests/`（新增 7 项单元测试 + 3 个 profile/calibration 工具）
- `模块B/clean_green_engineering_handoff_2026-05-09/tools/`（新增
  `fix_data_yaml_path.py`、`run_green_check.ps1`）

### 修改（语义/契约变更）
- `模块A/defense/module_a/alert_state.py`：`hold_frames=N` 现在真的 hold N 帧
- `模块A/defense/module_a/detector.py`：`_resolve_artifact_path` 支持
  `MODULE_A_ROOT` + 4 级回退
- `模块A/defense/module_a/features/temporal_texture.py`：新增 adaptive
  baseline EMA + persistence gate + 软封顶
- `模块A/defense/module_a/fusion/classifier_fusion.py`：新增
  `threshold_override` 参数
- `模块A/defense/module_a/features/a3/static_media/detector.py` /
  `config.py` / `feature_builder.py`：L0 多尺度从 4 quadrant + 5 次
  interpolate round-trip 改为 centre + 可选 4 quadrants 直接切
- `模块A/defense/module_a/features/a3/static_media/candidate_extraction.py`：
  接受 ndarray 输入避免重复 GPU→CPU；bg_edge 边缘 strip 直算
- `模块A/defense/module_a/features/{motion_artifact,blur_degradation,light_flow}.py`：
  ROI 循环的多次 `.item()` 合并成一次 `torch.stack().cpu()` 批量同步
- `模块B/clean_green_engineering_handoff_2026-05-09/repo/tests/test_hybrid_purify_config.py`：
  cwd 相对路径改为文件位置相对

### 删除
- `模块B/clean_green_engineering_handoff_2026-05-09/run_green_check.bat`
  （我上一轮留下的临时脚本，替换为 `tools/run_green_check.ps1`）
- `模块A/defense/module_a/features/a3/static_media/feature_builder.py`：
  无用的 `tensor.contiguous()` 冗余 GPU memcpy

## 关键数字

### 单元测试
| 仓 | 基线 | 夜班后 | 状态 |
|---|---|---|---|
| 模块 A | 0 | **64** | 全绿 ✅ |
| 模块 B | 122 | 122 | 全绿 ✅ |
| 联合根 `pytest 模块A\tests 模块B\…\tests` | n/a | **186** | 全绿 ✅ |

### 性能（7 clip 均值 p95）
| 特征 | 基线 p95 | 优化后 p95 | Δ |
|---|---|---|---|
| A3b | 15.3–18.0 | **8.9–11.5** | −40% |
| A3 (motion+blur+flow+track) | 3.3–6.7 | **2.7–4.4** | −35% |
| A2 (texture+temporal) | 1.8–3.8 | 1.9–3.4 | ≈ 0 |
| A4 | 0.7–1.2 | 0.7–0.9 | −15% |
| **Pipeline total** | 25.9–29.0 | **18.5–21.8** | **−25%** |

### 功能正确性（7 clip smoke）
| Clip | 告警帧（基线 / 夜班后）|
|---|---|
| clean_baseline | 0 → **0** ✅ |
| glare_attacked | 151 → 162 |
| motion_blur_attacked | 157 → 160 |
| occlusion_attacked | 173 → 173 |
| visibility_degradation | 145 → 145 |
| adv_patch_attacked | 498 → 504 |
| screen_spoof_attacked | 599 → 549 |

clean_baseline 上 `local_temporal_texture_change` 噪声
**3582 → 2173（-39%）**。

## 该怎么看这些结果

- **合并前阻塞项全部清除**：路径解析、Ultralytics 污染、B test cwd 依赖
- **最重的功能 A3b 降本 40%**，screen_spoof 场景从 24 FPS 理论可跑 35+ FPS
- **LBP 噪声抑制**让 clean 场景的告警 reason 列表好看很多（-39%）
- **A4 分类器可通过 config 调阈值**，不需要重训 artifact
- **joint_baseline.yaml + joint_run_smoke.ps1** 是合并后立即可用的骨架

## 下一步建议（合并当天做的事）

1. 复制 `探索/joint_baseline.yaml` 到联合仓 `configs/`，路径改成合并后的
2. 跑一遍 `探索/joint_run_smoke.ps1` 当作 CI smoke（A 7-clip + B green check）
3. A-11 Monitor_App 拆分（仅出了方案 `探索/A_monitor_app_split_plan.md`）
4. 联合决策 hook：A 的 alert_confirmed → 触发 B 的 runtime_guard 一次；
   B 的 helmet 检测填 A 的 `p_safety`

以上两类工作都已经在 `工作清单/README.md` 的 P4 节列出，随时可以恢复。

## 清单

`工作清单/README.md` 还在，`STATUS.md` 记录了每次改动的时间/内容/验证结果。
