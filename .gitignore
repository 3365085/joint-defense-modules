# Python 缓存/编译产物
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# Pixi 环境
.pixi/

# 运行时产物 / 证据 / 日志（写在源码树外，不入库）
# 用 /前缀锚定到仓库根，避免误伤 model/src/defense/runtime 等源码目录
/runtime/
/runtime_evidence/
/runs/
/reports/
/logs/
/model/runtime/
/model/runs/
*.log
_smoke_result.txt

# 大型本地素材 / 模型权重
/素材/
*.pt
*.onnx
*.engine

# 嵌套独立仓库 / 独立工作区 / 实验区（有自己的管理或全是产物，主仓不追踪）
rebuilt_demo/
b模块新算法/
purification_lab/
# doc/ 是早期文档归档（含大 csv/png/视频产物），当前文档统一在 docs/；旧归档不入主仓
/doc/

# codegraph 本地索引缓存 / 诊断研究输出 / 临时会话导出（本地产物，不入库）
.codegraph/
a3b_research_out/
New Session.md
/model/**/feature_probe.jsonl
/model/**/head_probe_frames/

# 密钥
*.env
.stock_keys.env
