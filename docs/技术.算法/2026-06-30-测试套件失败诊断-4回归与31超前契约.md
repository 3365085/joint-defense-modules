# 2026-06-30 测试套件失败诊断（35 失败：4 回归 + 31 超前契约）

## 背景

阶段0 跑全量 `pixi run smoke`：**333 passed, 35 failed, 3 skipped**。
关键事实：tests/ 52 个文件中 **51 个未被 git 跟踪**（demo 移植带入，此前从未入库）。
用 workflow 并行诊断 14 个失败文件的 35 个用例，区分"测试超前未实装"vs"提交引入回归"。

## 诊断结论

| 分类 | 数量 | 处置 |
|---|---|---|
| regression_from_commit | 4 | **修代码**（全部来自提交 3cd4966 legacy 调优） |
| test_ahead_of_code | 31 | skip 并标注"契约未实装" |
| flaky/env/unclear | 0 | — |

## 4 个真回归（提交 3cd4966 引入，必须修）

与提交时精读标注的高危项完全吻合，集中在两文件两处改动：

### A. target_anchored.py — strong_static_glare 短路抢占自然曝光抑制（2 例）
- `test_natural_exposure_does_not_trigger_without_target_anchor`（ratio=0.11 无轨迹）
- `test_natural_exposure_ignores_normal_motion_without_track_support`（ratio=0.31 正常相机运动）
- 根因：提交把 `if suppressed → natural_exposure_suppressed` 优先分支，改成
  `if strong_static_glare(写死阈值0.10) → overexposure` 在前、`elif suppressed` 在后（L217-236），
  导致两个本应被自然曝光抑制的场景被 strong_static_glare 短路抢先置 suspicious=True。
- 修复方向：恢复 `suppressed` 相对 strong_static_glare 的优先级；或让 0.10 阈值随
  `natural_exposure_max_ratio` 联动而非写死。

### B. a3b_soft_trigger.py — quality_gate 分数旁路（2 例）
- `test_a3b_soft_trigger_warns_but_does_not_confirm_strong_observation_without_candidate`
- `test_a3b_soft_trigger_warns_on_stable_observed_only_track`
- 根因：提交在 `_debug()` 新增 `if not quality_gate and observed_score>=0.55 and not failed: quality_gate=True`
  （L381-382）+ high_score_bypass（L191-198），把无候选帧强制标 quality_hit、清掉 `no_candidate_or_screen_cue`，
  使 observed_strong（要求 `not quality_gate`）和 observed_window 分支不可达。
  叠加：`from_mapping` 默认阈值（0.62/3/0.78）与 dataclass 字段默认（0.55/2/0.70）不一致。
- 修复方向：移除/收紧 `observed_score>=0.55 强制 quality_gate` 旁路；对齐 from_mapping 与 dataclass 默认。

> 这 4 个回归全在 **legacy 路径**（rebuilt 内核不走 target_anchored/a3b_soft_trigger 的这些分支）。
> 鉴于用户已定 legacy 调优 git 不可信、后续重审，可选直接回退 3cd4966 这两处高危改动而非顺调。

## 31 个超前契约（可 skip）

均为 demo 移植带入、生产代码从未实装（git `-S` 无命中）：
- `test_model_security_runtime.py`(6)：status 缺 next_action/source_pt_resolution，不猜 sibling pt、mark_trusted(report_hash) 未实装
- `test_ppe_display_tracker_person_state.py`(6)：整个 person_state 子系统不存在，构造即 TypeError
- `test_web_index_preview_state_contract.py`(3)：前端 operator_message/next_action/preview_overlay_baked 守卫未实装
- `test_web_overlay_timeline_contract.py`(3)：`_can_hold_track` 不识别 held_static/fresh_detection 血统字段
- `test_web_detection_readiness_contract.py`(3)：旧准入逻辑自动扫描/净化覆盖 admission，"仅准入"契约未实装
- `test_ppe_display_tracker_label_stability.py`(2)：无 helmet_switch_confidence 等强切换参数
- `test_ppe_display_tracking.py`(2)：DetectionFrameResult slots 无 diagnostics 字段
- `test_video_defense_pipeline_reuse.py`(2)：`_maybe_reuse_detections` 无 PPE敏感阈值/`_last_reuse_decision`
- `test_monitor_engine_shutdown.py`(1)：无 record_model_security_context 方法
- `test_ppe_tracking_lineage_and_context.py`(1)：as_dict 不输出 fresh_detection/display_box_source/lineage
- `test_runtime_file_preview_contract.py`(1)：runner 无 preview_overlay_baked
- `test_video_diagnostic_contract.py`(1)：`_run_detection` 硬编码 640×640，不读 preserve_aspect_preprocess

## 影响范围与结论

- 我的 6 个提交里，commit 3（legacy 调优）确实引入了 4 个回归，印证用户"legacy 调优不可信"判断。
- 核心检测逻辑（333 个 tracked 行为）全绿；rebuilt 内核路径不受这 4 个回归影响。
- 31 个超前契约是未来工作项（部分与北极星无关：person_state/web 文案/保宽高比预处理），不阻塞主线。
- 下一步：处理 4 个回归（修或回退）→ 31 个 skip 标注 → 全量 smoke 转绿 → tests/ 全部入库建立门禁。
